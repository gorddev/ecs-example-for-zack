import Location from "../../Location";

const NullLocation = new Location({
    name: "Test Location",
    description: "This is where all the zackholes gather.",
    type: "hole"
});

export default NullLocation;
